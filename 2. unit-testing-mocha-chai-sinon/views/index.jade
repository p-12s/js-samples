doctype html
html(lang=en)
    head
        title=title

    body
        h1="Color palette"
        each color in palette
            div.color(style="background-color: " + color + "; height: 100px; margin-bottom: 10px;")
                p=color